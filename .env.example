# ── Supabase ─────────────────────────────────────────────────────────────
# Leave these blank to run the portal in DEMO MODE (data stored in browser).
# Fill them in to connect the real Postgres / Auth backend.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Needed for admin user provisioning (never expose to client).
SUPABASE_SERVICE_ROLE_KEY=
